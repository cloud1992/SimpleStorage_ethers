{
  "dependencies": {
    "alchemy-sdk": "^2.9.2",
    "dotenv": "^16.3.1",
    "ethers": "^6.2.3",
    "fs": "^0.0.1-security",
    "solc": "0.8.7-fixed"
  },
  "scripts": {
    "compile": "yarn solcjs --bin --abi --include-path node_modules/ --base-path . -o . SimpleStorage.sol"
  }
}
